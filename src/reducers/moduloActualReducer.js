const initialState = {
    id:null,
    nombre:'',
    permisos:[],
    submodulos:[]
}

export const moduloActual = (state = initialState, action)=>{
    switch(action.type){
        
        default: 
            return state;
    }
}